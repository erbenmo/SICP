addition:
	width[result] = width[arg1] + width[arg2]

for multiplication, bounds are not deterministic (i.e. (max opt1 opt2 opt3...) (min opt1 ...))
(-3, 15) * (-2, 17) = (-51, 255)



jz:
0,10 * 0,2
vs
-5,5 * 0,2